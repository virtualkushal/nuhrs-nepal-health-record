Place your brand images here:

  logo.png  -> the NUHRS shield logo (shown in the header, AppShell, and auth pages)
  hero.png  -> the "Unified Health Data" isometric illustration (shown on the
               right side of the "One Network, Every Facility" band on the home page)

They are referenced as absolute paths ("/logo.png", "/hero.png"). If a file is
missing, the UI falls back to a Material icon so nothing breaks.
